apiVersion: chainsaw.kyverno.io/v1alpha1
kind: Test
metadata:
  name: wellknownuri-validation
spec:
  skip: false
  concurrent: true
  skipDelete: false
  namespaceTemplate:
    metadata:
      labels:
        istio-injection: enabled
  steps:
    - try:
        - create:
            file: authpolicy-invalid.yaml
        - assert:
            file: authpolicy-invalid-assert.yaml

