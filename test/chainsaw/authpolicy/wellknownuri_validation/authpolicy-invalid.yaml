apiVersion: ztoperator.kartverket.no/v1alpha1
kind: AuthPolicy
metadata:
  name: invalid-well-known-uri
spec:
  enabled: true
  wellKnownURI: https:///path
  selector:
    matchLabels:
      app: application

